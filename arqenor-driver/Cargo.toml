[workspace]
resolver = "2"
members = [
    "driver",
    "common",
    "client",
]

# arqenor-driver uses its own workspace because:
# - driver/ requires nightly + WDK toolchain
# - common/ must be no_std compatible
# - This workspace is excluded from the main ARQENOR workspace
