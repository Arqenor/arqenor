[workspace]
resolver = "2"
members = [
    "driver",
    "common",
    "client",
]

# sentinel-driver uses its own workspace because:
# - driver/ requires nightly + WDK toolchain
# - common/ must be no_std compatible
# - This workspace is excluded from the main SENTINEL workspace
